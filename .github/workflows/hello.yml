name: Say hello

on:
  push:
    branches:
      - ghactiontest

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Set up Node.js
      uses: actions/setup-node@v2
      with:
        node-version: '14'

    - name: Install dependencies
      run: npm install

    - name: Run tests
      run: 
        cd C:\Users\manue\OneDrive\Escritorio\proyectofinal\PI1Equipo1\backend\NaturePic\src\main
        npm install
        npm test